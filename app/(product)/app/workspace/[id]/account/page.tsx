// "Account" tab (J5 — "Manage my account"). In Phase A of the 5-tab IA this
// aliases the existing /settings surface so the customer-vocab URL renders
// today. Phase B folds in support tickets/threads and merges the autonomy +
// work-thresholds controls. /settings stays reachable until Phase C.
export { default } from "../settings/page";
