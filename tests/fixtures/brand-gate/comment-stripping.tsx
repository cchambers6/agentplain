// This comment mentions Claude, Anthropic, ChatGPT, OpenAI — must NOT trigger R1.
/* Block comment: SMB, seamless, leverage — must NOT trigger R4. */

// PLACEHOLDER mentioned in comment — must NOT trigger R2.

export function CleanSurface() {
  /*
   * Anthropic is the model provider. This is documentation, not customer copy.
   * Claude for Small Business is a competitor context we handle gracefully.
   */
  return (
    // Another line comment: ChatGPT question from prospects.
    <div>
      {/* JSX block comment: OpenAI, SMB, seamless — all suppressed */}
      <p>Intelligence rooted in reality.</p>
    </div>
  );
}
